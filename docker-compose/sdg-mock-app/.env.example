# All WIL/n8n credentials and CHEFS configuration are now stored
# per-playground in the SQLite database. No environment variables needed.
#
# Optional: override the SQLite database path (defaults to ./data/playgrounds.db)
# PLAYGROUND_DB_PATH=/path/to/playgrounds.db
