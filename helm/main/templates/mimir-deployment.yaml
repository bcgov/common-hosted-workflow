{{- if .Values.mimir.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "_.fullname" . }}-mimir
  labels:
    {{- include "_.labels" . | nindent 4 }}
    app.kubernetes.io/component: mimir
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "_.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: mimir
  template:
    metadata:
      labels:
        {{- include "_.labels" . | nindent 8 }}
        app.kubernetes.io/component: mimir
    spec:
      {{- with .Values.mimir.securityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: mimir
          image: {{ .Values.mimir.image.repository }}:{{ .Values.mimir.image.tag }}
          imagePullPolicy: {{ .Values.mimir.image.pullPolicy }}
          args:
            - -config.file=/etc/mimir/config.yaml
            - -config.expand-env=true
            - -activity-tracker.filepath=/data/metrics-activity.log
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
            - name: grpc
              containerPort: 9095
              protocol: TCP
          env:
            {{- toYaml .Values.mimir.extraEnv | nindent 12 }}
          {{- with .Values.mimir.containerSecurityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: config
              mountPath: /etc/mimir
            - name: data
              mountPath: /data
          readinessProbe:
            httpGet:
              path: /ready
              port: http
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
          resources:
            {{- toYaml .Values.mimir.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "_.fullname" . }}-mimir-config
        - name: data
          emptyDir: {}
{{- end }}
